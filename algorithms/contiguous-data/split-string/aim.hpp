/*
  The aim of this topic is:
    Split a string given a custom delimiter
    input string should never be empty
*/

#pragma once
#include <string>
#include <vector>

// Algorithm to be tested

std::vector<std::string> split(std::string const& in, char delim);

